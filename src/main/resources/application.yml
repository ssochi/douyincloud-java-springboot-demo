server:
  port: 8000

spring:
  redis:
    host: redis-cnlfws7cbbee694ma.redis.ivolces.com
    port: 6379
    password: e6aDarNPvwdhfq9      # Redis服务器密码
    database: 0
    timeout: 10000
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0

  datasource:
    url: jdbc:mysql://mysql75c46c48415a.rds.ivolces.com:3306/douyin_demo?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: yyyjsz
    password: e6aDarNPvwdhfq9
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
